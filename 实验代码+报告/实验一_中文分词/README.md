# 实验一：中文分词

本文件夹包含实验一所需的代码、数据和运行结果。

- `src/`：前馈神经网络中文分词代码
- `data/segmentation_corpus.txt`：从 `199801` 大语料抽取的分词句子，共 1200 句
- `outputs/exp1_segmentation_result.txt`：已生成的运行结果

运行命令：

```bash
python run_exp1.py
```
