# 实验二：句子相似度

本文件夹包含实验二所需的代码、数据和运行结果。

- `src/`：TF-IDF 句向量与余弦相似度代码
- `data/similarity_examples.txt`：从 `199801` 大语料抽取的真实句子，共 6 句
- `outputs/exp2_similarity_result.txt`：已生成的运行结果

运行命令：

```bash
python run_exp2.py
```
